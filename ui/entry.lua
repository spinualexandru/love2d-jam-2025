local main_menu = require('ui.main_menu')
local options = require('ui.options')

local ui_interfaces = {
    interfaces = {
        main_menu = main_menu,
        options = options
    }
}


return ui_interfaces
